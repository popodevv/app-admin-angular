import { Distributor } from './distributor';


describe('Distributor', () => {
  it('should create an instance', () => {
    const directive = Distributor;
    expect(directive).toBeTruthy();
  });
});


